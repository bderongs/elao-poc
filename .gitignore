# dependencies
node_modules/

# Next.js
.next/
out/

# env files (contain secrets — set these in Railway dashboard)
.env
.env.local
.env.*.local

# Large binary assets — serve from CDN or object storage instead
*.fbx
*.glb

# build artifacts
tsconfig.tsbuildinfo

# OS
.DS_Store

# graphify generated knowledge graph (not part of the app)
graphify-out/

# Internal project planning (not for client repo)
SCOPE.md
DEV-PLAN.md
.cursor/
