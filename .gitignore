# dependencies
node_modules/

# Next.js
.next/
out/

# env files (contain secrets — set these in Railway dashboard)
.env
.env.local
.env.*.local

# MCP server config — contains the Supabase access token
.mcp.json

# Large binary assets — serve from CDN or object storage instead
*.fbx
*.glb
# ...except the placeholder avatar, committed until it's moved to a CDN
!public/avatars/brunette-t.glb

# build artifacts
tsconfig.tsbuildinfo

# OS
.DS_Store

# structured server logs (Mistral call timing/errors) — local debugging only
logs/

# graphify generated knowledge graph (not part of the app)
graphify-out/

# Internal project planning (not for client repo)
SCOPE.md
DEV-PLAN.md
.cursor/

# Supabase CLI local cache
supabase/.temp/
.vercel
